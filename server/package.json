{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bunx wrangler dev && tsc --watch",
    "old_dev": "bun --watch run src/index.ts && tsc --watch"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250610.0",
    "better-auth": "^1.2.9",
    "drizzle-kit": "^0.31.1",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.7",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
